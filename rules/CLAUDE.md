# CLAUDE.md

本文件是给 Claude Code 的项目入口上下文。

**单一真相源**：本仓库所有行为准则以 `AGENTS.md` 为准。
- Claude Code 进入项目后，必须先阅读 `AGENTS.md` 再执行任何动作。
- 本文件只保留最短入口信息，避免重复与漂移。

## 必读
- `AGENTS.md`

## 项目定位
- 这是一个“AI 开发操作系统”工作站：通过 `AGENTS.md` + `.opencode/`（commands/skills）+ MCP，把 AI 的行为固化为可复现的流水线。

## 快速入口
- 规划与范式：`plans/ai-dev-paradigm-plan.zh.md`、`plans/ai-dev-paradigm-plan.en.md`
- Vibe Coding 方法论：`vibe-coding-cn/`
- OpenCode 教程：`learn-opencode/`
- 编排器实现：`oh-my-opencode/`

## 规则变更
- 修改规则时：只改 `AGENTS.md`，并同步更新本文件的入口指向。
