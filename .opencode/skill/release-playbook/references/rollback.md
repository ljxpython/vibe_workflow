# 回滚策略（通用）

目标：把“失败时怎么办”写成可执行步骤，而不是口号。

## 回滚分层
1) **代码回滚**：回到上一个已知可用版本（git commit / tag）
2) **产物回滚**：回滚前端静态资源（dist）或镜像版本
3) **数据回滚**：尽量采用向前修复；若可逆迁移必须验证可回滚

## 发布后验证（建议）
- 健康检查：服务可达、关键 endpoint 返回
- 关键路径：登录/核心业务流
- 监控：错误率、延迟、告警

## 必须输出（给 AI）
- 回滚触发条件
- 回滚命令序列
- 回滚后验证命令
